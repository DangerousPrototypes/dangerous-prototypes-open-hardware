Eclipse project. You may need to adjust JDK path in project settings.

Also this project could be compiled with Ant:
JAVA_HOME=/opt/java/jdk1.6.0_29/ ant build-all

To run it look for file ./target/dist/ftd2xxj (or *.bat on Windows).
